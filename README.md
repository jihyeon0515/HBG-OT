# 헬스보이짐 OT 관리 시스템 (hbgym_ot)

헬스보이짐 분당정자점의 신규회원 OT(오리엔테이션) 문진·배정·진행을 관리하는 **Flutter 웹앱**입니다.

## 워크플로

```
회원 문진표 작성 → 관리자 접수 → 트레이너 배정 → OT 진행/작성 → 완료 → 성공/실패
```

상태: `접수 → 배정완료 → 진행중 → 완료 → (성공 | 실패)`

## 역할별 화면

- **회원**: 신규 상담 문진표 작성(기본정보·방문 계기·운동목적·병력·운동경험·성격·특이사항) 후 제출
- **관리자**: 상태 배너로 분류 조회, 기간·종목·담당자 필터, 트레이너 배정, 트레이너별 보드, 완료건 성공/실패 처리
- **트레이너**: 배정 알림, 회원 작성 내역 조회, InBody·근력·자세·컨설팅·오티 3회차 작성 → 오티문진표 미리보기(로고·인체 이미지) → 완료

## 기술

- Flutter 3.44 (Web)
- 상태관리: provider
- 로컬 저장: shared_preferences (다기기 연동 전 단계 — 이후 Firebase 연결 예정)

## 실행

```bash
flutter pub get
flutter run -d chrome
# 또는 웹 빌드
flutter build web
```

빌드 산출물(`build/web`)을 정적 서버로 서빙하면 브라우저에서 사용할 수 있습니다.

## 데모 파라미터

- `?seed=1` : 샘플 데이터 시드
- `?role=member|admin|trainer` : 시작 역할
- `?view=list` : 관리자 화면을 목록형으로 시작

## 구글 드라이브 연동 (실시간)

상단 메뉴(⋮) → **구글 드라이브 연동** 에서 내 구글 드라이브의 모든 스프레드시트
(OT배정명단·오티/리뉴 관리표·마케팅관리표 등)를 실시간으로 조회할 수 있습니다.

- 브라우저 Google Identity Services(OAuth) 팝업으로 인증 — 백엔드 불필요
- Google Drive API(목록) + Google Sheets API(탭·셀 값)를 **읽기 전용**으로 호출
- 최초 1회 OAuth 클라이언트 ID 입력이 필요합니다 → **[GOOGLE_SETUP.md](GOOGLE_SETUP.md)** 참고

## 다음 단계

- 시트 데이터를 앱 내부 회원/OT 레코드로 가져오기(가져오기 매핑) 또는 쓰기 연동
- Firebase 연결(실시간 다기기 공유 + 트레이너 푸시 알림)
- 웹 배포(주소 발급), 이후 동일 코드로 안드로이드/iOS 앱

---
🤖 Generated with [Claude Code](https://claude.com/claude-code)
